# Auto-generated by Kelp. Safe to edit; Kelp only re-adds this file if it goes missing.
name: kelp/check
on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: Mic52M/kelp-action@v1